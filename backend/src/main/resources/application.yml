spring:
  datasource:
    # Configure via env vars to avoid hard-coding local DB assumptions.
    # Example: MYSQL_HOST=localhost MYSQL_PORT=3306 MYSQL_DB=omnischool
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:omnischool}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties.mail.smtp:
      auth: true
      starttls.enable: true

jwt:
  secret: ${JWT_SECRET:omnischool-secret-2024}
  expiration: 86400000
  refresh-expiration: 604800000

app:
  cors:
    # Allow Vite dev server + deployed site.
    allowed-origins: http://localhost:5173,http://localhost:8081,https://omnischool.tn

server:
  port: ${SERVER_PORT:8080}
